//! `context-index` (Graphify) — tree-sitter AST graph + LSP semantics + optional
//! embeddings, feeding token-budgeted context to the agent loop.
//!
//! Placeholder: implemented in Phase 3 of `docs/plans/ferrous-roadmap-spec.md`.

#![forbid(unsafe_code)]
