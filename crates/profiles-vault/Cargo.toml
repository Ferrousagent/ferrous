[package]
name = "profiles-vault"
description = "User profiles, master-password vault (Argon2 + sealed keyring), and encrypted secrets."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]

[lints]
workspace = true
