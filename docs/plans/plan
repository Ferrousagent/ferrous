Ferrous OS: A WASI runtime and built into the command line by default, acting as an operating system shell for human and AI collaboration.Isolated Workspace: Instant git/code-hosting integration that views settings, detects required packages, handles updates, acts as a harsh code reviewer, and provides an audit log for bugs or outdated packages.Built-in Cybersecurity: A lightweight, powerful security tool (similar to Trivy) that blocks malicious code and files before they enter the editor.AI Engine & Routing Elite Auto-Routing: A highly advanced system powered by a custom Rust calculator to route requests optimally.Smart Token Pipeline (COTP): Features smart token cutting (such as forcing COTP in JSON or YAML) to slash token usage and maximise performance, except for creative tasks.Mermaid Agent Routing: A fast .md or flowchart system for the AI to instantly calculate which agents are needed for specific tasks.Super Fast Autocomplete: Ultra-low latency local code completion running via an optimised local setup or fast API keys.Integrations & CapabilitiesMCP Marketplace: A built-in marketplace for Model Context Protocol integrations.Skills Centre: A dedicated hub for modular AI capabilities and skills.Local Rust Database: A local database tracking models, prices, account balances, TPM/RPM limits, and benchmarks with encrypted passwords and API keys by default.Free Unlimited Search Engine: A super lightweight search engine function for the AI (similar to SearXNG or a lighter alternative).Interface & UX/UI CanvasStage Editor: A CodeMirror 6 editor that allows you to toggle to the canvas for a full product view.Framer-Like UI/UX Canvas: A visual design interface based on Instackable (GitHub) that syncs code with a live preview. Uses strict mathematical design rules, Motion by Framer, and custom drag-and-drop blocks that both the human and AI must use.Terminal & Browser Layout: A terminal window positioned on the right for human and AI testing, utilizing a custom headless browser (similar to Vercel Agent Browser) with no multimodal capabilities required.Floating Chat & Navigation: A floating chat bar combined with a collapsible sidebar interface.Jarvis Voice Experience: A seamless Speech-to-Text (STT) and Text-to-Speech (TTS) loop using Kokoro-82M for real-time audio interaction. And make this projects. add graphify like stuff so ai can understand full codebase max accuracy whilst not hallucinating or burning trough tokens. Local Visual Engine (Manus-Kill Loop)Zero-Pixel Vision: Skips heavy screenshots, cloud streaming, and multimodal model requirements.JSON/YAML Canvas Mirror: Keeps a live text-based layout tree of your Instackable drag-and-drop design blocks.Direct-State Injection: The AI "sees" the UI via ultra-compact YAML data and "clicks" or "drags" items by emitting instant JavaScript event commands via the Tauri bridge.The Payoff: 100% accurate, 90% cheaper on tokens, lightning-fast execution, and works perfectly on completely offline local models.⌨️ Context-Aware "AI Tab" KeySingle Tap (In Editor): Inserts instant, ultra-low latency local inline code completions.Single Tap (On Canvas): Locks the AI agent's focus directly onto that specific visual component block and its graph dependencies.Double Tap (Anywhere): Summons your lightweight floating chat bar instantly at the current cursor position.Global Hold: Acts as a hard break-point interrupt to freeze a runaway running agent loop immediately. Specialised skills and subagents.

